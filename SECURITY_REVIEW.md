# Security Review

审阅日期：2026-08-11

## 范围

- 3 个原始 ZIP 包
- 14 个源文件与资源文件
- 文本、JavaScript、CSS、JSON、HTML 和 PNG 元数据

## 扫描结果

未发现以下内容：

- API Key、Access Token、GitHub Token
- 密码、私钥、证书或 `.env` 文件
- 数据库连接串或 Authorization Header
- 邮箱地址、手机号或真实业务数据
- PNG 作者、GPS 或设备元数据
- 隐藏文件、可执行二进制或压缩包路径穿越项

## 已处理问题

发现 3 处包含本机用户名的绝对路径，已改为通用的 `<project-root>` 占位路径：

- Design Token 文件路径
- Art Design Pro 源码说明路径
- 组件库 registry 中的源码路径

## 非密钥类风险

### Component Capture 数据范围

浏览器扩展会收集并提交：

- 所选元素的 DOM，`outerHTML` 最多保留 50,000 字符
- 递归子元素结构和计算样式
- 页面 URL 与标题
- 当前可见区域截图
- 用户手动填写的名称、描述、标签、状态和附加代码

默认提交目标是本机 `http://localhost:54321`。这些数据可能包含账号名称、内部 URL、页面文本或其他业务信息，因此不要在无授权页面或敏感系统中使用。

### 浏览器权限

扩展声明了 `activeTab`、`scripting` 和 `<all_urls>`。其中 `<all_urls>` 范围较宽，发布到扩展商店前建议验证是否可以仅依赖用户点击触发的临时页面权限。

### 远程服务

文档提到未来可替换为远程 API。启用远程提交前至少应加入：

- HTTPS
- 身份验证与权限控制
- 敏感字段过滤或脱敏
- 数据保留和删除策略
- 用户提交前的明确预览与确认

## 结论

清理本机绝对路径后，仓库中未发现可识别的凭据或个人隐私内容。当前主要风险来自扩展运行时可能采集网页内容，而不是仓库本身携带敏感数据。
